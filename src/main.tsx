import React from "react";
import ReactDOM from "react-dom";
import "tailwindcss/tailwind.css";
import TimeTrackingDashboard from "./time-tracker";

ReactDOM.render(
  <React.StrictMode>
    <TimeTrackingDashboard />
  </React.StrictMode>,
  document.getElementById("root")
);
